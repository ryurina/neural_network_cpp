#include <iostream>
#include <vector>


/*
 * input (x1,..., xn)
 * weights (w1,..., wn)
 * bias
 *
 * z = sum(wi*xi + b)
 * ŷ = Relu(z)
 *
 * error = y - ŷ
 *
 * lr (learning rate)
 *
 * n_iter (number of iteration)
 *
 * */

class Perceptron{
  public:
    double lr;
    double n_iter;

    Perceptron(double lr, double n_iter): a(a), b(b) {}
};

int main (int argc, char *argv[]) {
  
  return 0;
}
